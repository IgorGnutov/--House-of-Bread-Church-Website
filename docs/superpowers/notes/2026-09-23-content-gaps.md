# Дірки в контенті, виявлені при перенесенні (Етапи 1–2)

Перенесення було побайтовим: нічого з переліченого **не** виправлено в даних.
Це робота замовника на кроці «Актуалізація контенту» (див. дорожню карту). Рядки 11 і 17 позначено як завдання розробки для Етапу 2; рядки 13–16 — дірки перекладу (англійського тексту в легасі немає, вигадувати його не можна).
Рядки 18–22 додано при перенесенні сторінок на Етапі 2 (Задача 15); легасі-файли, на які посилаються старші рядки, видалено в кутовері — вони лишаються в історії git.

| # | Що | Де | Наслідок, якщо лишити |
|---|---|---|---|
| 1 | `geo.lat` / `geo.lng` відсутні | `contact-info`, усі 6 церков | Етап 3 не зможе видати розмітку `Church` з координатами |
| 2 | Усі 12 ресурсів лідерів мають `url: null` (у легасі `href="#"`) | `leader-resources` | сторінка для лідерів показує картки, які нікуди не ведуть |
| 3 | `tst.4.text` англійською коротший за той самий текст у `testimonies-data.js` (бракує «with family») | `homepage.testimonies.items[3]` проти `vitalii-s` | одне свідчення двома різними англійськими текстами на різних сторінках |
| 4 | `seo.*` порожні в усіх записах | усі колекції | Етап 3 дасть мета-теги з фолбеків, а не з контенту |
| 5 | 95 плейсхолдерів (`picsum.photos` + `images.unsplash.com`), 95 різних URL, 94 різні зображення (фото №9 повторене з іншою шириною): галереї служінь — 54, галереї церков — 15, галереї проєктів — 10, фото служителів — 11, картинки новин на головній — 3, постери відеосвідчень — 2. Перерахунок: `grep -rhoE 'https://(picsum\.photos\|images\.unsplash\.com)[^"]*' src/content \| wc -l` (додайте `\| sort -u` для різних URL; обріжте на `?` — `/[^?"]*` — для різних зображень) | усі галереї, фото пасторів і пресвітерів, новини головної, відеосвідчення | реальних фото немає, OG-картинки теж. Справжні лише `public/uploads/hero-cross.jpg`, `public/uploads/hero-cross-mobile.jpg` (фон героя) і логотип |
| 6 | `/about/`, `/contacts/`, `/donate/` мають лише заголовок із меню, `body: null` | `pages.about`, `pages.contacts`, `pages.donate` | поки `body: null`, маршрут узагалі не генерується (`isPageEnabled`, `src/lib/pages.mjs`) — сторінки в збірці й посиланні у футері немає; пункт меню веде на секцію головної, доки замовник не додасть прозу |
| 7 | Назва вулиці написана «Караманиць» (бракує фінальної «я») у `hero.addr` та у `contacts.addr`, а у `footer.addr` — правильно «Караманиця» | `contact-info.address.uk`, `homepage.hero.addr`, `homepage.contacts.addr` vs `homepage.footer.addr` | єдине джерело правди для адреси (яке буде використано footer, `/contacts/` і JSON-LD на Етапі 3) містить друкарську помилку |
| 8 | `con.addr` існує на двох місцях `index.html` з різними українськими текстами (повна адреса в контактах, коротка у footer), але одним англійським; `homepage.footer.addr` має коротку uk і довгу en | `homepage.footer.addr` | англійський footer показує іншу (довшу) адресу, ніж український |
| 9 | Пастор Дмитро Єгоров і пресвітер Андрій Коваль використовують одне й те саме фото Unsplash | `pastors/dmytro-iehorov`, `pastors/andrii-koval` | дві різні людини на одній сторінці з одним обличчям |
| 10 | Служіння `care` («Служіння турботи») має `icon: "globe"` у легасі даних, не `"care"` (який є гідним значком) | `ministries/care.json` | це, ймовірно, давня помилка; картка показує глобус (навмисне залишено як є) |
| 11 | Проєкт `social-canteen` має `ctaUrl: "index.html#contacts"` — посилання в легасі single-page сайт | `projects/social-canteen.json` | після Етапу 2 цієї URL більше не буде; має стати `/contacts/` (це — завдання розробки для Етапу 2, не контентний елемент) **Закрито на Етапі 2 (Задача 2):** `ctaUrl = "/#contacts"` |
| 12 | Три картинки новин мають `alt: ""` — у легасі вони декоративні (заголовок картки поруч) | `homepage.news.items[].image` | якщо картинка несе зміст, якого немає в заголовку, скрінрідер його не озвучить; замовник має підтвердити «декоративна» або дати опис |
| 13 | **Переклад:** alt фону героя «Підсвічений синім хрест у храмі» є лише українською, англійського немає ніде в легасі | `homepage.heroImage.alt` | англійська головна матиме український alt |
| 14 | **Переклад:** доступні підписи інтерфейсу є лише українською, англійського немає ніде в легасі. Розмітка `index.html`: aria-label «Головна навігація», «Відкрити меню», «Мобільне меню», «Закрити меню», «Мобільна навігація», «Сума пожертви, грн», «Прокрутити вниз», «Попереднє свідчення», «Наступне свідчення»; title карти «Карта — House of Bread Church». `church/ministry/project.dc.html`: aria-label «Хлібні крихти»; `church/project.dc.html`: «Попередній слайд», «Наступний слайд»; `ministry.dc.html`: «Попереднє фото», «Наступне фото»; `pastors.dc.html`: «Ел. пошта», «Телефон». Згенеровані скриптом: aria-label «Слайд N» (`church/ministry/project.dc.html`), «Перейти до слайда N» (`church.dc.html`); title iframe відео «Відео церкви», «Відео проєкту», «‹назва служіння› — відео» | розмітка й скрипти легасі-сторінок; у типізованих даних місця для них немає, бо немає другої мови | англійські сторінки озвучуватимуть скрінрідером українські підписи **Оновлено на Етапі 2 (рішення 15):** англійські підписи додано в `src/i18n/{uk,en}.json` під `a11y.*` з перекладом розробника — переклади очевидні («Previous slide»), але **не погоджені замовником**; окремого рядка для погодження немає, він тут |
| 15 | **Переклад, частковий:** українські підписи, англійський варіант яких у легасі є, але на **іншій** сторінці: бейдж «Відео» в галереях `church/ministry/project.dc.html` (англ. `Video` є лише в `projects.dc.html` → `badge.video`); на головній alt «Відеосвідчення» і aria-label «Дивитися відеосвідчення», а також title iframe «Відеосвідчення» на головній і `testimonies.dc.html` (англ. є в `testimonies.dc.html` → `testimony.videoLabel`, `testimony.watch`) | uk.json/en.json | Етап 2 може перевикористати ці ключі, але це розширення перекладу за межі легасі — потребує згоди замовника |
| 16 | **Переклад:** усі 103 `alt` у `media[]` (служіння, церкви, проєкти) лише українські — у легасі англійських alt немає, `mediaItem.alt` — рядок, не пара | `ministries`, `churches`, `projects` → `media[].alt` | англійські сторінки деталей матимуть українські alt; якщо замовник дасть переклади, `alt` стане парою `{uk, en}` (завдання розробки) |
| 17 | Українські значення `news.more` і `donate.quote` містять розмітку (іконка-стрілка `<svg>`; вкладений `<cite data-i18n="don.ref">` з переносом рядка), англійські — ні: легасі читає текст через `innerHTML`, а при перемиканні на англійську затирає розмітку | `homepage.news.more.uk`, `homepage.donate.quote.uk` | мовні версії одного поля мають різну структуру: Етап 2 має вирішити, чи рендерити ці поля через `set:html`, чи винести іконку й `<cite>` у шаблон; список полів з HTML — коментар біля схеми `homepage` у `src/content.config.ts` (завдання розробки) **Закрито на Етапі 2 (Задача 2):** розмітку прибрано з даних, стрілка й `<cite>` у шаблоні |
| 18 | `homepage.pastors.role` / `role2` більше не використовуються — ролі пасторів на головній беруться з колекції `pastors` (рішення 14) | `homepage.pastors` | зайві поля переїдуть у Storyblok, якщо не прибрати до Спеки 3 |
| 19 | Адреса месенджера (`https://m.me/dom.hleba.org`) і пошта координатора (`leaders@houseofbread.church`) — у шаблонах, не в даних | `pastors.astro` (`MESSENGER`), `leaders.astro` (`COORDINATOR`) | редактор не змінить їх без коду; кандидати в поля `help.url` для Спеки 3 |
| 20 | Лічильники «6 файлів» / «6 ресурсів» — статичні рядки словника | `docs.count`, `res.count` | при додаванні сьомого ресурсу лічильник збреше; потрібні форми множини для uk |
| 21 | `footer.built` — «Побудовано на Next.js + PayloadCMS» | `homepage.footer.built` | неправда про стек сайту на кожній сторінці |
| 22 | **UX, не контент:** стрічка Facebook у `#media` головної після завантаження стає вищою на ~296px (так само в легасі), тож якорі нижче `#media` зсуваються вже після прокрутки — посилання «назад» на якорі нижче `#media` (напр. `/#pastors`) можуть приземлитися не на секцію | `src/pages/[...lang]/index.astro`, блок `.fb-page` | користувач повертається «назад» і бачить не ту секцію; виправлення — зарезервувати висоту стрічки, але це зміна вигляду, рішення відкладено |
