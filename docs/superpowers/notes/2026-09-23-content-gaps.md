# Дірки в контенті, виявлені при перенесенні (Етап 1)

Перенесення було побайтовим: нічого з переліченого **не** виправлено в даних.
Це робота замовника на кроці «Актуалізація контенту» (див. дорожню карту). Один рядок позначено як завдання розробки для Етапу 2.

| # | Що | Де | Наслідок, якщо лишити |
|---|---|---|---|
| 1 | `geo.lat` / `geo.lng` відсутні | `contact-info`, усі 6 церков | Етап 3 не зможе видати розмітку `Church` з координатами |
| 2 | Усі 12 ресурсів лідерів мають `url: null` (у легасі `href="#"`) | `leader-resources` | сторінка для лідерів показує картки, які нікуди не ведуть |
| 3 | `tst.4.text` англійською коротший за той самий текст у `testimonies-data.js` (бракує «with family») | `homepage.testimonies.items[3]` проти `vitalii-s` | одне свідчення двома різними англійськими текстами на різних сторінках |
| 4 | `seo.*` порожні в усіх записах | усі колекції | Етап 3 дасть мета-теги з фолбеків, а не з контенту |
| 5 | 92 плейсхолдери (`picsum.photos` + `unsplash.com`): 81 у галереях служінь, проєктів та відеосвідчень, 11 фото служителів | усі галереї, фото пасторів і пресвітерів, відеосвідчення | реальних фото немає, OG-картинки теж |
| 6 | `/about/`, `/contacts/`, `/donate/` мають лише заголовок із меню, `body: null` | `pages.about`, `pages.contacts`, `pages.donate` | сторінки збудуються порожніми й не ранжуватимуться (це вже зафіксовано в дорожній карті) |
| 7 | Назва вулиці написана «Караманиць» (бракує фінальної «я») у `hero.addr` та у `contacts.addr`, а у `footer.addr` — правильно «Караманиця» | `contact-info.address.uk`, `homepage.hero.addr`, `homepage.contacts.addr` vs `homepage.footer.addr` | єдине джерело правди для адреси (яке буде використано footer, `/contacts/` і JSON-LD на Етапі 3) містить друкарську помилку |
| 8 | `con.addr` існує на двох місцях `index.html` з різними українськими текстами (повна адреса в контактах, коротка у footer), але одним англійським; `homepage.footer.addr` має коротку uk і довгу en | `homepage.footer.addr` | англійський footer показує іншу (довшу) адресу, ніж український |
| 9 | Пастор Дмитро Єгоров і пресвітер Андрій Коваль використовують одне й те саме фото Unsplash | `pastors/dmytro-iehorov`, `pastors/andrii-koval` | дві різні людини на одній сторінці з одним обличчям |
| 10 | Служіння `care` («Служіння турботи») має `icon: "globe"` у легасі даних, не `"care"` (який є гідним значком) | `ministries/care.json` | це, ймовірно, давня помилка; картка показує глобус (навмисне залишено як є) |
| 11 | Проєкт `social-canteen` має `ctaUrl: "index.html#contacts"` — посилання в легасі single-page сайт | `projects/social-canteen.json` | після Етапу 2 цієї URL більше не буде; має стати `/contacts/` (це — завдання розробки для Етапу 2, не контентний елемент) |
